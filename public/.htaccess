<IfModule mod_rewrite.c>
    RewriteEngine On

    # Si el archivo o directorio existe, se sirve directo
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Todo lo demás pasa por index.php
    RewriteRule ^ index.php [QSA,L]
</IfModule>
